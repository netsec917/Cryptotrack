import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#ff2975',
    },
    background: {
      default: '#3c345c',
    },
    text: {
      primary: "#ff2975",
      secondary: "#ff2975"
    },
  },
});

export default Theme;